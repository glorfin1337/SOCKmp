#include "headers/SOCK.h"

int Server::CreateServer()
{
	// socket bağlantısı oluştur ve geri döndür
	return 1;
}

int Server::ReadData(int sock)
{
	return 1;
}

int Server::CloseServer(int sock)
{
	// socket bağlantısını kapat
	return 1;
}